`[2026-09-12] 0.12.0 DeepSeek 断流根修（野生恢复页）+ 登录真因透传 + z.ai 误报根修 + 登录态持久化 + 命名简化 | lib/browser-driver.js, lib/web-control.js, lib/index.js, lib/providers.js, lib/client.cjs, test-mock/real-verify.mjs | 四条真机反馈闭环；doctor 8/8`

CDP 取证定位的四个真机根因（探针沉淀在 test-mock/cdp-peek|netprobe|workerprobe|
xhrprobe|freshcap|loginfeature.mjs，全部经 DevToolsActivePort 只读连真机）：
- DeepSeek 240s 超时：轮次落在 Edge 会话恢复冒出的「野生页」上，捕获 binding 与
  init script 均未注入（__webcodeChunk undefined），网页端回复完整生成、SSE 流
  完全可读（responseType=text，progress 正常）而桥收不到任何字节。三层修复：
  launch 永远 ctx.newPage() 干净页 + 关闭全部恢复页 + --hide-crash-restore-bubble；
  installPage 尾部注入自检（失败重装再失败报警）；每轮发送前 captureChainAlive
  自检，死捕获换干净页。超时错误携带页面现场（捕获死活 + 未回传回复字数）。
- 豆包/Kimi「请求失败」：openLogin 循环 page 变 null 后 page.isClosed?.() 直接
  TypeError（真机 27s/10.7s 失败）；且 login 路由把错误放 message 字段，面板
  api() 只认 data.error → 吞成「请求失败」。修复：失败响应带 error 字段；等待
  循环判空先行 + 自愈重开一次（cookies 在 profile），重开失败按可读错误收场。
- z.ai 未登录误报「登录完成」：游客页 textarea+发送按钮齐全，「有输入框=已登录」
  必然误报（loginfeature 探针实测）。统一判定 judgeLoggedIn + 站点级 loginProbe
  契约（z.ai bad=游客页可见「登录/Sign in」按钮），openLogin/openWindow/
  gotoFreshChat/runTurn 就绪判定全部收口。
- 重启后全部「待检查」：loggedIn 是进程内存值。各站点 profile 落盘
  webcode-login-state.json，driver 与未懒创建站点双路回读，面板显示
  「已登录(缓存)」，即时核验仍走登录/检测。
- 面板：右栏镜像拦截说明换短句、与连接错误不再同屏重复；模型描述去「网址 ·」
  前缀对齐其他供应商简洁风（DeepSeek（深度思考）/ GLM-5.3 / Kimi K3 …）。
- doctor 对齐 0.11.0 单模型目录：flash=deepseek 别名 wantThink 恒 true；期望
  model_type=null 时按驱动 strict 核验同语义跳过（原先拿实际值比对 null 必挂）。

`pnpm test` 全绿；真机 doctor 8/8（DeepSeek 发送→捕获→解析→回传闭环 + 元数据
核验）。安装时 profile 依赖 dsh-smooth-stream@0.6.1 未满 24h 最小发布年龄被
pnpm 供应链策略拒——按官方 Exclude 名单同型追加 0.6.1 后安装成功（不等待）。

安装后逐站点真机验证再揪三类站点级根因（全部当轮修复当轮复验）：
- **GLM 断流**：埋点 SDK 把 window.fetch 恢复成原生引用（installed=true 而包装
  出链）→ captureInit 重写为 `__wcCap` 特征标记 + 500ms 守护查丢重装；GLM
  复验 200/3.8s 含思维链。
- **Qwen 双断**：流端点迁 `/api/v2/chat/completions`（completionPaths 补
  `/chat/completions`）+ 发送需点 `.send-button`（Enter 不触发）+ decoder 适配
  （无 finish_reason，以 delta.status='finished' 收束；思考在
  extra.summary_thought.content）→ 复验 200/OK-QWEN 含思维链。
- **z.ai loginProbe**：`:text-matches` 对嵌套 span 按钮不命中，改 `has-text`
  → 复验快速诚实 NEED_LOGIN。kimi/doubao profile 无有效登录态，同样诚实报
  NEED_LOGIN，面板点「登录」即可。
- longrun（10 轮真实工具闭环）PASS：同一网页会话增量续跑、13 次真实工具执行、
  跨轮记忆回读精确（5930 行）、增量与块内容一致。
- 供应链策略与网络：dsh plugin add 因 minimumReleaseAge 拒装 →
  pnpm-workspace.yaml 的 minimumReleaseAgeExclude 追加 0.6.1（官方同型）；
  github release 网络不稳时用「解包覆盖 node_modules + package.json file: 声明
  更新 + FILES-MATCH 校验」安装（0.11.0 同法）。
