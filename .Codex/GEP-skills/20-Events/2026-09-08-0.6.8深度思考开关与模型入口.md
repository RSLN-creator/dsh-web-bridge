---
type: event
date_occurred: 2026-09-08 23:30
capsule_used: ""
---
# 事件:0.6.8 深度思考手动开关 + 站点模型入口重构

## 背景
- 用户反馈两点:①其他网址的模型选择名字错了(编目录时照抄参考仓库,未核实网页现状)②希望能手动控制「深度思考」开关,不要全自动。
- 另:此前已定位网页版思考链根因——「深度思考」是 composer 区独立 pill(aria-pressed),专家模式(model_type=expert)不自动开思考,pill 开启后 thinking_enabled=true、THINK 流完整回传(0.6.5 syncThinkPill 已自动双向同步)。

## 执行
[2026-09-08] 深度思考手动三态 | lib/index.js、lib/openai.js、lib/browser-driver.js、lib/client.cjs | settings.thinkMode auto/on/off;OpenAI 前端支持请求级 think_mode;buildTurn→meta→executor→driver thinkOverride 全链;设置页与 /__webcode/settings-page HTML 均加三态下拉。
[2026-09-08] 站点模型入口重构 | lib/providers.js、lib/browser-driver.js | 非DeepSeek 8站点(未真机校准)改 site:auto「网页当前模型」;GLM 网页实测已到5.x(旧目录4.5/4.6过时);auto 入口不点任何模型 UI,网页手选为准。
[2026-09-08] 真机验证 | test-mock | think_mode=off 压住专家模式(reasoning None+pill false);think_mode=on 给 flash 强开(reasoning 有+pill true);38 测试绿;0.6.8 装入 DSH web profile 后 /__webcode/models 目录正确。

## 踩坑(新)
- client.cjs 手改 React 树时 h(...) 括号闭合容易少一个——new Function 语法检查必须每次跑;报错行号是解析器放弃点,不是错误点。
- pnpm pack + install 偶发 corepack 栈错误静默失败(装的还是旧版),必须 grep node_modules 里实际版本确认。
- Get-CimInstance 按 CommandLine 匹配 node 进程会因引号形态漏掉目标;杀 DSH 后端用 Get-NetTCPConnection -LocalPort 3080 找 OwningProcess 最可靠。
- 多个 dsh web 实例并发会 EADDRINUSE 互相顶;换版本重装后必须杀干净再起单实例。

## 结论
- ✅ 深度思考三态(自动/始终开/始终关)全链生效,用户可手动控制。
- ✅ 未校准站点模型名不再撒谎;DeepSeek 三档保留真机验证的精确契约。
- ⏭️ 逐站真机登录校准后,可把 site:auto 细化为真实模型清单。
