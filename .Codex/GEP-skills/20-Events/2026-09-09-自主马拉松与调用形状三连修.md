# 2026-09-09 自主马拉松与调用形状三连修

[2026-09-09] 0.6.9 装入 DSH + 测试方案升级 + 三缺陷修复 | package.json(0.6.8→0.6.9 tgz 安装于 ~/.dsh/profiles/web)、lib/agent-preset.js、test/regression.test.mjs、test-mock/real-probe-17-autonomous-marathon.mjs | 0.6.9 装入 web profile（导入冒烟+deepseek 契约核对+real-verify 8/8）；测试改无外部输入长时自驱动+轨迹留档+对比官方 API 轨迹（08-14~08-24 deepseek-official 3 会话基准）；首跑暴露裸 <invoke> XML 调用被丢（第 3 轮裸奔）→ invokeRe 归一修复；终跑再暴露 DSML 全角竖线/丢头噪声 → strip 归一（对齐 reference/deepseek-free-api）；探针 shell maxBuffer 1MB→16MB（ENOBUFS 虚高 errRate）；终跑 PASS（14 轮自驱动、31 调用、errRate 9.7% 全恢复、思维链 10276 字）；调用形状容错 4→7 种、回归 40/40 绿；对比报告 doc/research/autonomous-marathon-vs-official-api-2026-09-09.md；commit 3debfbc。踩坑：zstd 流式读必须循环 read()（一次 read(n) 返回截断）；probe 超时遗留孤儿 node→Edge 进程树锁 profile（杀根 node 即解锁，taskkill 对权限外子进程无效）。
