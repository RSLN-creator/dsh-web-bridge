---
type: event
date_occurred: 2026-09-08 21:15
capsule_used: ""
---
# 事件：0.6.0 全面修复（47 分钟潦草轮的返工）

## 背景
- 用户报告四个问题：①模型设置界面没法选择②二级界面选择也没有③设想（多站点/默认模型）未实现④接收网页端信息（思考链/图片）又搞没了。要求逐阶段 git 提交、尽量少跑测试、增强自身能力。

## 根因
- 用户安装的是 0.5.1 tgz——上一轮的思考链/图片链路（relay onThink/onImage → index.js reasoning 块）全在工作树未打包，装进 DSH 的还是旧版。
- 工作树 executor 的 sendTurn 分支（DSH 会话模式主路径）只透传 onDelta，丢 onThink/onImage——即使打包了会话内也不会有思考链。
- client.cjs 设置区无任何模型选择控件（只有静态文字"快速/识图/深度思考"），登录按钮不传 siteId。
- settings 的 defaultModel/previewRefreshRate 只存不用：buildTurn 从不读 defaultModel。

## 执行
[2026-09-08] 修复链路 | lib/index.js、lib/web-control.js、lib/client.cjs | executor sendTurn 补传 onThink/onImage；GET /__webcode/models 全站点目录；connect 按 siteId 路由 driverFor。
[2026-09-08] 默认模型生效 | lib/index.js | buildTurn 读 configManager defaultModel（settings.defaultModel || cfg.modelId）。
[2026-09-08] 设置界面可选择 | lib/client.cjs | ModelSelect（optgroup 按站点分组，标注深度思考/识图/实验）+ 保存按钮；SiteRows 各站点登录状态+按站点登录；侧栏站点切换条（/ __webcode/site/<sid>/ mirror 路由，index.js mirrorFor 懒建每站 mirror）。
[2026-09-08] 回归测试 | test/regression.test.mjs | +3：sendTurn 分支思考链/图片到达流（reasoning-delta + markdown 图）、默认模型生效；38/38 绿（本轮唯一一次测试运行）。
[2026-09-08] 0.6.0 打包安装 | package.json 0.6.0 + tgz；~/.dsh/profiles/web file: 依赖指向 0.6.0 tgz，pnpm install 完成，导入冒烟（ModelSelect/SiteRows/siteConnect/onThink 全在位）。

## 结论
- ✅ 四项全部修复并安装；分两阶段提交（c0f72a2 修复主体、9d9ec06 打包）。
- ⏭️ DSH 真机 GUI 长任务验证（思考链/图片/工具调用/多站点选择）进行中。
