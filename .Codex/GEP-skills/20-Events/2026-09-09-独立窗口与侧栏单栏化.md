# 2026-09-09 独立窗口与侧栏单栏化（0.7.0）

[2026-09-09] 右栏 UI 三项改造 + openWindow 独立窗口机制 | lib/browser-driver.js（openWindow/closeWindow/windowState、CDP Browser.setWindowBounds 停靠屏幕右半、launch/installPage 拆分、页 close 自愈）、lib/web-control.js + lib/index.js + bin/bridge-standalone.js（POST/GET /__webcode/window、windowOpener、sites[].window）、lib/mirror.js（侧栏单栏化：导航列特征探测收为 overlay 抽屉、左缘右滑/Ctrl+B/左缘 rail 呼出）、lib/client.cjs（站点 pill 横向滚动+独立窗口按钮+浮窗美化+抽屉开合动画） | 版本 0.6.9→0.7.0（tgz 装入 ~/.dsh/profiles/web，全文件 sha 对源码 MATCH，含上次未打包的 agent-preset 七容错）；单测 40/40 绿；真机冒烟 5 项 PASS（临时 profile openWindow 有头渲染/CDP 停靠修复（SystemInfo.getInfo 须 browser-target session）/closeWindow 回无头/mirror 注入 6 检查/window 路由+CSRF 403/GLM 站点链路）。注意：DSH 需重启加载 0.7.0；GLM 独立窗口默认全新 profile 未登录，窗口内登录一次即与桥共用会话。
