---
type: event
date_occurred: 2026-09-07 18:50
capsule_used: ""
---
# 事件：真机完整工具调用闭环打通（正面回应"擦边"）

## 背景
- 用户上传此前会话轨迹批评"现在只能是擦边、empty response from web AI、工具调用格式不对"，要求"正确调用、用项目中的、新开对话测试工具调用"。

## 执行过程

[2026-09-07] 构建真实工具闭环探针 real-probe-11-tool-loop.mjs
- 技术: 复用 lib/agent-preset.js 的 serializeFirstTurn/parseAgentReply 协议，driver.sendTurn 保持同一网页会话（fresh→增量），自建只读白名单执行器（read/grep/pwsh），真实本地执行后回填 result 围栏。
- 操作: test-mock/real-probe-11-tool-loop.mjs（新增）；沿用已登录 .edge-real-profile。
- 功能: 三步闭环：R1 首轮注入 preset+自用"审查本项目安全性"案例 → R2 回填 → R3 收束。

[2026-09-07] 三轮真实跑通，发现两个执行语义差异
- 技术:
  1) 工作目录：网页用相对路径 "package.json"，执行器 cwd 基准须固定为实际工程目录，否则 read 定位失败 → 网页会坚持重试同一 read（正确 agent 行为但绕圈）。
  2) 正则语义：网页产出的 grep pattern 是 PCRE/grep 风格 `(?i)(token|...)`，JS `new RegExp` 不识别内联修饰符，需剥离为构造 flag。
- 操作: test-mock/real-probe-11-tool-loop.mjs 内 runTool 修 cwd 基准与 (?i)(?s) 归一。
- 功能: 完整闭环：网页自主 read(package.json)→成功；自主 grep(鉴权关键词)→命中 6 文件；回填后网页转总结："依赖仅 playwright-core 与 ws，未发现硬编码凭据，当前无明显安全风险"，干净收束不再无谓调用。

## 结论
- ✅ 协议教学 + 增量回填在真实 DeepSeek 网页完整可用；网页能自主决定调用并干净收束，否决"擦边"。
- ✅ 解析器对网页真实的相对路径参数、PCRE 正则已归一处理。
- ✅ 固化统一契约自检 test-mock/real-verify.mjs：真机 7 项全绿（登录连通、flash=default、deepseek=expert、vision=vision+识图、工具闭环自主 read→真实执行→总结收束）。vision 偶发超时应设为短超时+独立容错，失败如实 FAIL 不误报，服务端对短时多次图片上传有限速。
- ✅ 跨平台增强：browser-driver defaultEdgePath 在 Windows 路径后追加 macOS/Linux 常用 Edge 路径（Windows 顺序不变，零回归）。
- ⚠️ 工具执行器（cwd、正则方言）的语义差异需真实 harness 一侧同样归一，是本工程与生态的共性需求。
- ⏭️ 下一步：脱敏 SSE 样本沉淀进供应商契约；跨平台 cfg（target_os/mac/linux 类 feature）完整化需在真正 mac/linux 环境验证。