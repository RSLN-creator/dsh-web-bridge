# 2026-09-10 长跑可靠性五修与审查入口（0.7.2）

[2026-09-10] DeepSeek 网页桥长跑可靠性五修 + 审查方法落地 + 版本库去噪 | lib/browser-driver.js（会话丢失抛 WEB_SESSION_LOST 不再静默改开新会话发增量；发送后回读 inputValue 校验 PROMPT_TRUNCATED；page crash / ctx close 立即 failActive 并自愈；中止等停止按钮点完且无契约时不再回落 div[role=button]；生命周期移入 installPage；删死代码 SEL/fillAndSend）、lib/index.js（executor 捕获 WEB_SESSION_LOST 用 meta.rebuild 重放整段首轮；localAnswer 收紧为只认 purpose=session-title；只出图判空修正；设置每轮读一次；sessionState 真 LRU；queueTimeoutMs 300s→900s）、lib/agent-preset.js（工具描述 300→1200 字符 + 平台说明）、lib/flatten.js（删无人引用的 flattenGenerateOptions/parseToolCall 与 v1 工具协议）、lib/web-control.js、test/regression.test.mjs（+3 项）、package.json 0.7.1→0.7.2、新增 doc/review-guide.md 与 doc/deepseek-longrun.md | 动机：用户要求「审查时不被老代码打扰」「说清 DeepSeek 网页真实长时间怎么跑」。查证安装本体（%APPDATA%\npm\node_modules\@deepseek-ai\dsh\config\agent-presets\）得到关键事实——DSH 系统提示词只有 harness identity 一句 + persona，工具规则全在 API tools 数组的 description 里（最长实测 397 字符，含 "Use native Windows paths" 等硬约束）；极简模式 persona 为 complete:true 的 "You are a helpful software engineer assistant."，即整条系统提示词就这一句，且无上下文压缩。本机 ~/.dsh/settings.yaml 默认恰为 minimal + webcode/deepseek:flash，是长跑最不利组合，故桥的首轮预设（agent-preset.js）是工具规则的唯一载体，截断描述=截断规则。验证：npm test 20/20、parse 12/12、run-m1/run-m4/run-m2d PASS；run-m2/run-m2b/run-m2c 失败项在改动前干净树上同样失败（既有欠账，已在 review-guide.md 标注为勿查）。版本库去噪：test-mock 下 9911 个再生浏览器 profile 取消跟踪（--cached，盘上保留）+ .gitignore 阻断增长，.git 由 427MB 不再扩张。commit 0d15d4b、b8d1366。
