---
type: event
date_occurred: 2026-09-07 21:20
capsule_used: ""
---
# 事件：真机回归「右侧浏览器查看」镜像代理 gate

## 背景
- 用户核心诉求 #1："参考 dsh-better-sidebar 右侧展开的浏览器"。mirror.js 实现该镜像代理；本 session 前几轮聚焦模型层，需给 mirror 补当前环境的真机回归证据。

## 执行过程

[2026-09-07] 运行真实 M3 gate
- 技术: 核查发现 mirror 已有单测（mirror.test.mjs 断言 data-webcode-mirror 注入）+ 真实 gate run-m3-mirror.js。直接运行后者。
- 操作: node test-mock/run-m3-mirror.js。
- 功能: 验证镜像转发真实工作。

[2026-09-07] 结果（5/5 PASS）
- 技术:
  - login token probe：无 relay 时降级 unauthenticated boot check（design 如此）；
  - mirror 转发上游 index.html：status 200, bytes=10683；
  - x-frame-options 被剥离 → iframe 可嵌入；
  - bootstrap 注入（data-webcode-mirror）；
  - 真实 SPA 经 mirror 加载渲染，title="DeepSeek"。
- 操作: 输出 M3 RESULT: PASS。
- 功能: 右侧浏览器的转发/可嵌/boots 链路真机成立。

## 结论
- ✅ 「右侧浏览器查看」镜像链路当前环境真机回归通过；composer 深度交互需 relay 注入 token + 已登录/鉴权 profile（生产场景），本 gate 降级判定按设计通过。
- ⏭️ 跨平台 cfg 与多站点需真实对应环境；harness 侧 UI/导入/列表在插件仓库之外。