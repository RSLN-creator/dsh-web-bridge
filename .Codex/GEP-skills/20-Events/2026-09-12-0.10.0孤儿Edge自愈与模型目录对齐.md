# 2026-09-12 · 0.10.0 孤儿 Edge 自愈 + z.ai API 代理 + 模型目录对齐

`[2026-09-12] 0.10.0 孤儿 Edge 自愈（CDP 优雅回收 + WMI 强杀）+ 控制面错误透传 + z.ai API 域代理 + 模型目录对齐 2026-09 网页版（1M 档） | lib/browser-driver.js, lib/web-control.js, lib/providers.js, lib/index.js | 「开过一次豆包后全站打不开」「z.ai 无法连接服务」修复；模型上下文按新网页版生效；DSH 已重启`

## 真机取证

- 豆包 profile 有 9 个孤儿 msedge（旧会话被 taskkill /F 强杀的桥留下），lockfile
  EPERM；launch 报「Target page, context or browser has been closed」。
- Stop-Process/taskkill 对这些进程**拒绝访问**（受限 DACL）；
  `Invoke-CimMethod Terminate`（WMI）成功终结（9→0）。
- z.ai 镜像页加载但前端调绝对域 https://api.z.ai 的后端接口跨域失败 →
  「暂时无法连接到服务」。
- 用户侧现象：设置面板逐站 ✗ 请求失败（旧 web-control 把真实错误吞成固定文本）。

## 修复

1. browser-driver.js：launch args 加 `--remote-debugging-port=0`（profile 记录
   DevToolsActivePort）；锁冲突 catch 里三层自愈：connectOverCDP → browser.close
   优雅回收 → WMI Terminate 强杀 cmdline 含本 profileDir 的孤儿 → 清锁重试一次。
2. web-control.js handle() catch：err.message 截断 200 字符透传给设置面板。
3. providers.js：zai staticOrigins += api.z.ai；模型目录更新
   （deepseek/GLM-5.3/Kimi K3/Qwen4 架构 1M、Claude 200K、ChatGPT 196K、
   豆包/Grok 256K），models 条目加 context 字段并贯通 listAllModels/resolveModel
   （模型条目 > contextWindowBySite > 64K 兜底）。
4. mirror.test.mjs / multi-site-decoder.test.mjs 护栏同步新契约。

## 验证

- E2E：doubao 开窗 → 强杀桥 node（留孤儿）→ 重启桥 → 再开窗成功。
- zai：bootstrap ASSETS 含 api.z.ai；/__static/api.z.ai/... 返回上游网关响应
  （`{"code":500,"msg":"404 NOT_FOUND"}`，非 host 拒绝）。
- `pnpm test` 全绿（M1 PASS）；0.10.0 打包 tgz 解包覆盖装 profile（sha diff 一致），
  DSH 重启后 /__webcode/models 返回新目录，3080/8931 正常。
