---
type: event
date_occurred: 2026-09-07 12:10
capsule_used: ""
---
# 事件：m4 长自用工具循环案例与全基线复绿

## 背景
- `/goal` 要求真实解决、完美适配、优先参考 agentsdock 等，且"每个工具的实际调用都要测试"。
- 上一轮遗留 m4 长自用安全审查探针 1 处断言失败、m2d 1 处断言失败，需定位根因并修复，恢复全绿基线。

## 执行过程

[2026-09-07] 基线盘点
- 技术: 运行 m1/m2/m2b/m2c/m2d/m3/m4 全量；m1/m2b/m2c/m3 通过。
- 操作: 全量测试跑批；确认 m2 的 EADDRINUSE 起源于用户真实运行的 `dsh web` 占用 8931，为环境冲突非回归。
- 功能: 定位仅 m2d、m4 各 1 项断言失败。

[2026-09-07] 根因定位
- 技术: 两处失败均为「测试夹具数据漂移」而非代码回归。
  - m2d 的 `planLine` 用 `l.includes('# dsh-webcode-bridge')` 找 PLAN.md 标题，但 PLAN.md 已改名为 `# Harness Web Bridge 路线`，回退到 slice(0,100) 不含标记。
  - m4 的 search 工具以 `ri?sky` 在工作区根匹配，但工作区没有 risky/rsky 文件，matches=0。
- 操作: read 测试源码 + 独立 node 验证 filter 逻辑（readme|plan 命中 2 个）。
- 功能: 确认逻辑正确、仅测试数据过期。

[2026-09-07] 修复测试夹具
- 技术: m2d 改 planLine 取首个 `# ` 标题、断言改查 `# Harness Web Bridge`；m4 改 search pattern 为 `readme|plan`（命中 README.md/PLAN.md）。
- 操作: test-mock/run-m2d-session.js、test-mock/run-m4-security-review.js。
- 功能: m2d、m4 均恢复 PASS。

[2026-09-07] 核心模式源码通读
- 技术: 完整读 index/agent-preset/browser-driver/flatten/decoder/providers；确认增量工具回填与 SSE 解码（THINK 不泄漏、RESPONSE 流式）逻辑正确。
- 操作: lib/*.js 全读。
- 功能: 基线确认：视觉、深度思考、并行 agents、授权持久化主干已具备。

[2026-09-07] 应对真实模型"不实际调用工具/思考浅"的 preset 强化
- 技术: agent-preset 增加「# 使用准则」段（依赖真实数据才调用、不编造结果、不堆砌也无谓省事）+ 传输协议追加"判定即立即调用、拿到结果即简洁收束、不继续无谓思考"。
- 操作: lib/agent-preset.js。
- 功能: 缓解用户反馈的"只有明确让做才做、思考浅/一直思考"。

## 结论
- ✅ m1/m2b/m2c/m2d/m3/m4 + regression8 + parse8 全绿；m2 仅环境冲突。
- ✅ m4 长自用案例 verify read→search→grep→最终安全总结，每个工具真实本地执行。
- ⚠️ 真实深度思考/识图行为需登录 chat.deepseek.com 实机验证，受限于本机无法可靠自动化登录，未在本会话做实机探测。
- ⏭️ 下一步：连通真实账号做实机探测各模式；跨平台/cfg 审查。