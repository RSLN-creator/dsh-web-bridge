# 2026-09-11 · 0.9.9 多站点 tab 适配 + 设置页重构 + GLM 累积帧去重

`[2026-09-11] 0.9.9 多站点镜像全量同源化 + GLM 累积帧去重 + iframe 保活 + 设置页重构（登录管理置顶/去历史导入/CSS 速度图/子代理模式） | lib/mirror.js, lib/decoder.js, lib/providers.js, lib/client.cjs, lib/settings-page.js, lib/index.js, bin/bridge-standalone.js | kimi/qwen/glm/chatgpt 右栏与设置面板按真实需求适配；0.9.9 已装 profile 并重启 DSH 生效`

## 真机取证（standalone 桥 8955 + 重启后的 DSH 8931）

- 镜像白名单漏域：qwen 的 `assets.alicdn.com`、glm 的 at/o.alicdn.com 等未进
  staticOrigins → crossorigin 脚本被拒 → tab 空白/渲染出错（镜像 200 但页面死）。
- standalone executor 写死 deepseek 驱动：zai:auto/glm:auto 全 MODEL_SITE_MISMATCH。
- GLM 流帧 text 为累积全文：真机一句话输出两遍（末两个帧各带全文）。
- client.cjs Settings 卡片括号错乱：连接卡片提前闭合，登录管理渲染不出来。
- Conversation 每次挂载 `?ts=` 重设 src：侧栏每开合一次整页重载数秒。

## 修复

1. mirror.js：属性语境与 CSS url() 的绝对资源 URL 全量同源化（内网 host 拒绝防
   SSRF）；providers.js 补 qwen/glm 实测域；mirror.test.mjs 锁新契约（公网未声明
   域 502=尝试代理、内网 404）。
2. bridge-standalone.js executor 按 meta.siteId 路由；glm:auto 真机一句话对话通。
3. decoder.js GlmDecoder 按 part:content 槽位累积差分（相同跳过/前缀扩展发后缀/
   无关追加），构造器透传 options；累积流夹具 + 原夹具全绿。
4. client.cjs：iframe 常驻保活（display 切换）；Settings 重构——账户与登录管理
   置顶（每站点 状态+登录+独立窗口）、模型管理、CSS 条形速度图、会话与子代理
   （subAgentMode own/share，index.js buildTurn keyPath 尊重）、历史导入 UI 移除。
5. index.js defaultConfig + settings-page.js 同步 subAgentMode。

## 验证

`pnpm test` 全绿（mirror 4/4、multi-site-decoder 10/10、stream-tail 6/6、
regression 34/34、M1 PASS）；重启后 DSH 8931 真机核验：settings 带 subAgentMode、
login-sites 返回 10 站点、qwen 镜像标签属性级外部引用归零。安装：pnpm 策略
（minimumReleaseAge）冻结重装，改用 tgz 解包覆盖 node_modules/dsh-webcode-bridge
（version + sha diff 校验一致），profile 依赖指针同步 0.9.9；`dsh web` 重启，
插件四行挂载日志正常（cordis 报错属 dsh-smooth-stream，与本插件无关）。

已知问题：zai:auto SSE 为 chat:completion 包装帧，openai-sse 解码器不匹配 →
单轮 240s 静默超时，需真机抓流后写专用解码器；chatgpt.com WAF/登录墙建议
独立窗口打开。
