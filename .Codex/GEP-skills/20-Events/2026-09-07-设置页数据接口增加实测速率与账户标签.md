---
type: event
date_occurred: 2026-09-07 19:20
capsule_used: ""
---
# 事件：设置页数据接口增强——暴露实测生成速率与账户标签

## 背景
- 用户要求设置页优雅化（账户、启动自动化、估算速度等），并指出"网页端生成速度和估算有很大区别"。核实发现设置页 UI 由 harness 原生渲染，本库只提供数据接口；`/bridge/status` 此前的 usage 只有 `estimateTokens` 估算，缺实测速率；账户区只有布尔 loggedIn。

## 执行过程

[2026-09-07] 核实设置页归属与缺口
- 技术: grep 定位设置页 UI 不在本库（openai.js/relay.js/index.js 无 text/html 设置页，只有 /bridge/*、/__webcode/preset 数据接口）。0.5.0 已含 consent 持久化、阶段指标（首字/思考/正文）与"实测/估算区分"。
- 操作: 只读核查 openai.js/relay.js/index.js/provider cockpit。
- 功能: 明确本库对设置页的优化边界是数据接口。

[2026-09-07] 补实测速率字段（纯增量，向后兼容）
- 技术: `runTurn` 的 metrics（endToEndMs/firstResponseMs/thinkingMs/responseMs）原先只进单次返回值；改为 `Object.assign(lastFinished,{metrics,chars})` 并新增 `status().lastRate`（含 charsPerSec=chars/responseMs、mode）；`publicStatus` 透传 `lastRate` 并新增派生 `account` 标签（已登录/未登录，不泄露真实用户名）。
- 操作: lib/browser-driver.js（metrics 挂 lastFinished + status 增 lastRate）、lib/openai.js（publicStatus 增 account+lastRate）。
- 功能: 设置页"估算 vs 实测"对比拿到真实上次生成速率（首字延迟/生成字符每秒），账户区有可读状态。

[2026-09-07] 真机验证
- 技术: real-probe-12-status-rate.mjs 真实发送一轮，读 status().lastRate。
- 操作: 已登录 .edge-real-profile，flash 单轮。
- 功能: endToEnd=877ms、首字=528ms、response=349ms、charsPerSec=34，lastRate 完整返回，LOAD_RATE OK。单测/回归全绿。

## 结论
- ✅ 设置页"实测 vs 估算"有真实数据源；账户区有可读标签；改动纯增量、真机验证、回归绿。
- ⚠️ 设置页 UI 本体及"默认模式/刷新率"等首选项持久化在 harness 侧（PLAN #4 剩余项），不在本库范围。
- ⏭️ 下一步：脱敏 SSE 样本沉淀进供应商契约；多站点/跨平台在真实环境验证。