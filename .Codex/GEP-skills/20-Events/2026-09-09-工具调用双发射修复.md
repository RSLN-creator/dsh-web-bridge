# 2026-09-09 工具调用同 id 双发射修复（0.7.1）

[2026-09-09] deepseek 工具调用双发射修复 + 真机复验探针 | lib/index.js（pendingCall 收尾复用同 index/id 补发参数并关闭，不再另开新块重发；driver 日志注入时显示 (injected)）、test/regression.test.mjs（新增 2 项：单调用块唯一性断言、流式多调用首块复用断言）、test/run-m1.js（双发射容忍正则收紧为严格单发射）、test-mock/real-probe-18-executor-double-fire.mjs（新增：apply()+真实网页流式+adapter.stream 完整链路复验）、package.json 0.7.0→0.7.1 | 根因来自真实会话 f3fa97fd（deepseek:flash）DSH 存档取证：每个工具调用被执行两次（先空参数 ToolArgsError INVALID_ARGS 假错误、再真参数），且 glm 会话 dcaba20a 文本三连重复是网页端行为不属桥缺陷。盲区实锤：马拉松探针自带执行循环、单测 scriptedDriver 一次性返回全文不调 onDelta，都不经 index.js 流式 pendingCall 路径——纯代码测试全绿但真机翻车。真机复验双通道 PASS：probe-18（R1 无同 id 双块/无空参数终块/执行器恰一次、R2 回填后正常收束返回真实 package.json 内容）、real-verify 8/8。0.7.1 tgz 装入 ~/.dsh/profiles/web，13 运行文件+client.cjs sha 全 MATCH（package.json 字段重排属 pnpm 安装归一化），DSH 重启生效。commit a9fe1ff。
