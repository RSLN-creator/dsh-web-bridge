---
type: event
date_occurred: 2026-09-07 20:40
capsule_used: ""
---
# 事件：真实网页多工具（read+grep）自主调用闭环验证

## 背景
- 此前真机工具闭环只验证了 read 一个工具；用户要求"每个工具的实际调用都要测试"。补验证 grep 的网页自主调用、正则参数、PCRE 归一与结果回填。

## 执行过程

[2026-09-07] 真实网页 read+grep 多工具闭环
- 技术: real-probe-14-multi-tool-loop.mjs 用真实已登录 profile + deepseek(专家) 模式；tools=[read,grep]；dispatcher 提供两个真实只读执行体（read 读文件，grep 递归检索并剥离 PCRE 内联修饰 (?i) 为 JS 正则）。案例引导网页"审查依赖安全性：read package.json + grep 检索 token/secret/password → 总结收束"。循环回填结果直到网页不再调用。
- 操作: 新增 test-mock/real-probe-14-multi-tool-loop.mjs。
- 功能: 真实多工具自主调用闭环跑通。

[2026-09-07] 断言与结果
- 技术: calls={read:1,grep:1}；summary 收束"项目依赖配置正常，源码中未发现硬编码 token/secret/password 等敏感凭证；本项目安全性审查通过"。
- 操作: node test-mock/real-probe-14-multi-tool-loop.mjs。
- 功能: read/grep 均真实执行、结果回填、干净收束，PASS。

## 结论
- ✅ 多工具（read+grep）在真实网页的自主调用闭环成立；正则 query（含 PCRE 内联修饰）经归一后真实检索。补齐了"每个工具都测试"的 grep 缺口。
- ⚠️ 网页对工具数量/顺序是按需决策，不保证每次都用全部工具；契约断言以“至少 read 成功 + 收束”为稳定面，多工具证据由本探针提供。
- ⏭️ 后续：跨平台 cfg 与多站点需真实对应环境；harness 侧 UI/导入/列表在插件仓库之外。