# 2026-09-11 工具调用折叠：外观对齐官方 ToolRow

[2026-09-11] 折叠外观对齐官方工具块 | lib/client.cjs 的 TF_CSS 与 header DOM 重做（24px 行高、
13px 次要文字色、hover 高亮、圆点分隔符、胶囊状态标、16px 前导记号），配色改走 DSW 主题变量；
test/toolfold.test.mjs 增一条外观护栏；版本 0.9.1 -> 0.9.2；pnpm pack 出 dsh-webcode-bridge-0.9.2.tgz。

起因：用户觉得 `▶ ⚙ pwsh command: ...` 太素，要求对齐 DSH 里官方灰度工具块的观感。

参照物（从本机安装的官方包读出来的，不是猜的）：

- `@deepseek-ai/dsh-client-ui-tool` 的 `ToolRow.module.css`：行高 `calc(24px + --dsh-content-font-delta)`、
  字号 `--dsh-content-font-size-secondary,13px`、title 用 `--dsw-alias-label-secondary`、
  summary 用 `--dsw-alias-label-tertiary`、分隔点是 2px 圆点 `--dsw-alias-label-caption`、
  错误态 `--dsw-alias-state-error-primary`；leading 是 16px 盒子装 14px 图标。
- `VARIANT_ICONS`：read/search/edit/code/others 各自一个 16px 线性图标，14px 渲染。
- 运行中的行有一条 2.6s 的 sweep 高光（本版没抄：折叠行没有 `running` 态的信息）。

改了什么（只碰外观，判定与摘要逻辑一行未动）：

- 头部：`display:flex`、无边框无底色，hover 才出 `--dsw-alias-interactive-bg-hover`；
- 前导：8px 的 caret 方块改成 CSS 画的 chevron（收起 -45deg，展开 45deg），后面跟 16px 记号位；
- 记号：调用齿轮 / 成功勾 / 失败叉，失败走错误色（原来是 `mark + name` 挤在一个 span 里）；
- 工具名与摘要之间加圆点分隔符，摘要独立成 `.hwb-tf-args` 省略号截断；
- 状态标从描边方块改成胶囊（`border-radius:999px` + `color-mix` 淡底）；
- 主题变量全部带回退色（`var(--dsw-..., #xxxxxx)`），脱离 DSH 主题也不会变成透明/黑色。

边界不变：仍在 DSH 对话流折叠、仍只切 `<pre>` 的 display、原文仍在 DOM 里、
普通代码块 / 散文举例 / 输入框内的代码都不碰。

实测：test/toolfold.test.mjs 9/9 绿（新增一条外观护栏：记号形状、分隔符数量、样式表里确实
引用了主题变量）；metrics / mirror / mixed-text-tool / multi-site-decoder / regression /
thinking-image / parse 全部 exit 0；`node --check lib/client.cjs` 通过。

未完成：真机 DSH 界面复验（本会话起不了浏览器）；profile 里装的仍是 01:03 的旧副本，
且 `dsh plugin list --profile web` 因 pnpm store index（sqlite）打不开而失败，安装待用户侧处理。
