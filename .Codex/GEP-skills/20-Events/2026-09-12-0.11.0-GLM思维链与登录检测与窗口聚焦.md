`[2026-09-12] 0.11.0 GLM 思维链解码 + verify-login 登录态检测 + 独立窗口聚焦/错位 + 模型命名网址化 | lib/decoder.js, lib/web-control.js, lib/browser-driver.js, lib/providers.js, lib/client.cjs, lib/index.js | 已登录站点思维链可见、登录态可验证、窗口跳回不覆盖；DSH 已重启生效`

真机取证（test-mock 新增三个探针 + WEBCODE_SSE_DEBUG 抓包）：
- GLM-5.3 思考内容 = content[].type='think'，字段 think，过程纯增量（28078 字符
  861 个 delta），finish 帧带累积全文，content 索引帧间漂移 → GlmDecoder 按
  type 差分，三形状（增量/累积/片段）全兼容，用例锁进 multi-site-decoder。
- z.ai：Enter 不触发发送（挂死根因）→ 契约补 #send-message-button；
  guest 会话不启动生成（发送/建会话/气泡正常）——生成流解码待真实账号。
- GLM composer：.think-mode-trigger（GLM-5.3极致）；qwen：.qwen-thinking-selector
  （自动）；z.ai：「深度思考 最高」默认开——深度思考默认值改为 on。
- 孤儿锁自愈补强：exitCode=21 纳入触发条件；profileDir 统一反斜杠再匹配
  （混合分隔符导致 WMI 杀进程匹配失败的坑）。
- 登录态验证：POST verify-login（connect 幂等回查）+ 设置面板「检测」按钮。
- 窗口：GET window 聚合 windows map；alreadyOpen → bringToFront 聚焦；
  新窗 36px/个错位停靠。
- 模型名：chat.deepseek.com · DeepSeek（1M）/ chatglm.cn · GLM-5.3 / kimi.com ·
  Kimi K3 / chat.qwen.ai · Qwen4 架构 / chat.z.ai · GLM-5.3-Flash 等。

`pnpm test` 全绿；0.11.0 tgz 解包覆盖装 profile（FILES-MATCH），DSH 重启后
/__webcode/models 返回新目录。client.cjs 曾被误写为 index.js 内容（cwd 漂移），
从 profile 副本恢复后用 Edit 工具重放编辑——教训：跨目录 shell 操作后必须先 pwd。
