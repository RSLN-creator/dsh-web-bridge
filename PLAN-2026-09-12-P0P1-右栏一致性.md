# PLAN · 2026-09-12 · 右栏一致性 P0/P1 执行计划（外部评审收口版）

状态：P0 待动工；P1 已获外部评审批准（附设计要求 A/B/C）；P2 搁置不立项。
文档性质：这是「此刻」需要的执行计划——只覆盖当前决策态下的行动项，不复写 `PLAN.md`（0.12.0 路线图，[D1]）已完成内容。

**唯一约束（[U2]）：本文档每个细节必须标注真实来源。来源共七类：[C]=本仓代码（2026-09-12 逐条核验）、[T]=本机实测（脚本+输出在本仓 .tmp/）、[R]=参考仓库代码（reference/ 已落地）、[D]=仓库文档、[V]=外部评审原文（已存档可溯源）、[O]=真机观察留痕、[U]=用户指令原文。无来源的判断不写入。**

---

## 0. 背景（一段）

右栏 iframe 镜像（127.0.0.1:8931 反向代理）与 Playwright 驱动页是两套隔离浏览器世界，用户要求「真实登录的网站打开没问题 + 登录状态一致 + 会话中同步看到，且必须是网页、不能截图中转」（[U1]）。外部评审已收口：P0 照准、P1 准（附 A/B/C 设计要求）、P2 缓议（[V-裁定表]）。评估阶段两处事实修正成立：镜像 cookie 通路实为双向打通（[C2][C3]），前缀 cookie 硬伤实为 rewriteCookie 剥 Secure 所致（[C1]），且回环上保留 Secure 已实测可行（[T1][T2]）。

## 1. 硬约束（不可谈判项）

| # | 约束 | 来源 |
| --- | --- | --- |
| U1 | 右栏必须是真网页（站点原生 HTML 经同源镜像），**禁止截图/CDP screencast 中转路线** | [U1] |
| U2 | 计划文档每个细节必须带真实来源（本文档的存在理由） | [U2] |
| U3 | `pnpm pack→重装→重启 GUI` 的执行时机由用户决定，不得为让改动生效而擅自重启 | [U3][O5] |
| U4 | 交付以真机端到端验证为准，单测全绿不算数 | [U4][D2] |
| U5 | 凭据只进 relay 进程内存；relay 永远绑回环（127.0.0.1:8931 + loopbackOnly 双保险） | [C5][V-B] |

## 2. P0 · 立即执行（零架构改动）

### P0-0 前置：外部评审存档
把评审全文存为 `reference/local-refs/2026-09-12-review-sources/external-review-2026-09-12.md`，使 [V#] 引用可溯源。来源：[U2][V0]。

### P0-1 四态登录徽标（右栏 sitebar）
- 改动点：`client.cjs` Conversation 组件 sitebar（[C10]，现状 tab 无任何登录信息）复用设置页 SiteAccounts 的数据通路（[C9]：`api('status')` → `driver.sites`，轮询先例 [C11]）。
- 四态而非两态：实时已登录 / 已登录(缓存) / 未登录 / 待检查(未初始化)；数据源实时优先、落盘 `webcode-login-state.json` 兜底（[C7][C8]；四态定义 [V-D]）。
- qwen 型「陈旧」样式：状态文件与实时接口说法不一时不显示确定态（现象留痕 [O2]）。
- 验收：十站点 tab 徽标与设置页「登录/检测」结论一致；doubao 缓存态不再误报为可用（[V-D]）。

### P0-2 未初始化/不可达站点的条件挂载 + 引导页
- 改动点：`client.cjs` ensureFrame 前先判登录态与探活结果（[C10]），不可达站点显示 stub 引导页而非镜像 502 错误页；探活结果会话内缓存（[V-Q6]）。
- 副作用红利：常驻 iframe 保活池（[C10]）不再为注定 502 的站点占资源（[V-Q6]）。
- 验收：chatgpt/grok/gemini/claude 四 tab 显示「本机网络不可达/地区受限」引导而非错误页（不可达判定来源 [O1]）。

### P0-3 部署脚本 + build 指纹
- 改动点：`pnpm pack → 重装 → 重启` 收敛为一条脚本；插件把 build hash 暴露到 status 接口（落点 `web-control.js` 'GET status'，[C11]），面板角落显示版本指纹（[V-E]）。
- 背景来源：「改了没生效」已实际咬人——0.12.1 tgz 已 pack 但已装副本哈希核对为旧文案（[O5]）；pack/install 坑位清单（同版本重装被跳过、corepack 静默失败、tgz 解包覆盖 profile 的路径写法）见记忆与 D3 Event。
- 验收：重装后面板指纹与 workspace build hash 一致。

### P0-4 client-render 第二用例修复（P1 的门禁）
- 先现场复跑 `node --test test/client-render.test.mjs` 确认现状（用例「窗口状态切换全程稳定」09-12 下午留痕为红 [O4]；文件存在 [C23]），红则修复后再动 P1 的 client.cjs 改动——红测试上不叠新改动（[V-E]）。
- 验收：`node --test test/` 全绿 + doctor（test-mock/real-verify.mjs [C23]）真机通过。

## 3. P1 · 评审已批准（附设计要求 A/B/C）

### P1-0 前置 spike：cookie 通道二选一（半天）
- 方案甲「镜像 jar 保留 Secure」：rewriteCookie 改为保留 Secure（仅去 domain），靠回环 trustworthy origin 落盘前缀 cookie。依据：[T1][T2]（Edge 150 实测可落盘可回传）+ [R3]（成熟代理只动 domain/path 不动 Secure）+ [V-修正2 nuance]（改名是陷阱，保留 Secure 才兼容 NextAuth 按死名字读取）。
- 方案乙「wabac 式 preset-cookie 无 jar」：上游 Set-Cookie 存 relay 内存，按 name=value 在后续上行请求头回放，浏览器 jar 彻底不参与。依据：[R2]（wabac.js response.ts:89-105 同构机制）。
- 判定标准：两方案各对 glm 真机跑通登录态注入后，按「单边腐化面」（甲有 jar 会演化需回写，乙无 jar 但 relay 内存成为第二真相源）、风控信号（甲的 cookie 属性可见于浏览器、乙完全服务端）、实现量对比定稿（对比框架 [V-B][R2][T1]）。
- 产出：设计决定 + 记录进本文档附录，不达标不进入 P1-1。

### P1-1 profile cookie 上行注入（glm 首发，deepseek 对照组不动）
- 改动点：`browser-driver.js` 用 `ctx.cookies(origin)`（playwright-core API，[C21] types.d.ts:10227）定期导出站点 cookie；`mirror.js` handle() 组装上游头时合并覆盖（现仅在 [C2] 转发 iframe 自带 cookie）。
- 站点顺序：glm 首发（唯一「今日核验登录+镜像 200+状态无歧义」的干净对象 [V-Q1]；GLM 条目 [C22]）；deepseek 保持 token 路不动——它是唯一端到端可用的基线，换机制等于把对照组一起改（[V-Q1]；现状 token 注入 [C4][C6]）；doubao 第二但先初始化驱动刷新 cookie；qwen 先对账（[O2]）、kimi 先登录，均无首发条件（[V-Q1]）。
- 已知风险：localStorage 路线对 httpOnly 会话站是结构性死路（JS 读不到）——这正是选 cookie 注入的机制级理由（[V-Q1]）。
- 验收：glm 镜像 iframe 打开即登录态视角，能看到驱动侧会话历史（真机截图留痕 [U4]）。

### P1-2 cookie 轮换回写（评审 B，防「单边腐化」）
- 机制：镜像收到的上游 Set-Cookie 逆重写后经 `context.addCookies` 回写驱动上下文（playwright-core API [C21] types.d.ts:10099）；不做则「驱动带旧 cookie 静默降级成强制重登录」迟早发生（[V-B]；单边腐化机制成立依据：[C3] 轮换 cookie 只进镜像侧）。
- 已知实现坑：回环源上 CDP cookie 查询报告不全（[T3]）——回写与导出都以 https 站点源为准操作，验证以请求头/页面行为为准，不依赖 CDP 读数做唯一判据（[T3] 结论外推，spike 中复核）。
- 验收：glm 镜像活跃 24h 后驱动侧无 401 激增（双源门禁见 P1-9）。

### P1-3 UA 对齐（评审 A）
- 改动点：`mirror.js` browserFingerprintHeaders（[C15]，已有 DEFAULT_UA 兜底位）改为「镜像上行 UA 统一改写为驱动 Edge 当前 UA」（driver 页 UA 可从 `page.evaluate(navigator.userAgent)` 取，[C17] 驱动页在手）。
- 理由：同 cookie + 异 UA 是风控经典信号（[V-A]）。
- 验收：镜像上行请求 UA 与驱动页 UA 逐字节一致（探针留痕 [U4]）。

### P1-4 rewriteCookie 前缀兼容（若 P1-0 选方案甲）
- 改动点：[C1] 剥 Secure 行为改为保留（仅去 domain；SameSite=None 在回环 trustworthy origin 上实测可留 [T1]）。
- 来源：[T1][T2][R3][V-修正2 nuance]。方案乙则此项消失（由 P1-0 定）。

### P1-5 镜像默认硬只读 + 显式手动模式
- 实现：`mirror.js` bootstrap 通道（[C16]，注入脚本机制现成）加 composer 遮罩 + capture 阶段拦 Enter/submit；**不做网络层一刀切禁非 GET**（会误伤心跳/在线轮询，把镜像页自己搞坏 [V-Q2]）。
- 诚实边界：这是防误操作硬、防蓄意绕过软（[V-Q2]）。
- 理由链：cookie 注入后镜像侧写入=同账号写入，不锁是给轮换互踩开正门（[V-Q2]）；镜像手动发送对驱动/捕获链完全不可见（架构事实：捕获 binding 只注入驱动页 [C17][C18]）；手动用网页独立窗口本就是更优界面（[C13] 同一持久 profile 同一页面）。
- 手动模式开启时若该站有活跃 turn：警告或暂停该站 agent 轮次（[V-Q2]）。
- 验收：镜像内输入框不可误触；开手动模式后可操作且活跃 turn 有提示。

### P1-6 每站 turn 锁（评审 Q5 提入 P1）
- 活跃 turn 期间 UI 层拦人为操作（独立窗口/镜像两侧的输入面），与 P1-5 闭环（[V-Q5]）。
- 理由：人工干扰是「功能上线必然出现」的丢失形态——独立窗口今天就可操作（[C13]），用户在途生成时切会话/聚焦输入框，流断而捕获链对这一切不可见；P1 的 cookie 注入、手动模式都在给它加触发面（[V-Q5]）。
- 验收：turn 进行中操作输入面被拦且有提示；真机 longrun 中人为干扰不再产生 silent 断流（[U4]）。

### P1-7 守护分类日志（评审 Q5 提入 P1）
- 改动点：现有断流三类诊断（decoder reason → driver throw 链 [C19]）之上，把每次 `no_response_frames`/超时的判定分类写进 incident 日志；`WEBCODE_SSE_DEBUG` 原始流落盘开关已存在（[C18]）。
- 方法论：没有分类标注，猜「第四类」就是打地鼠（[V-Q5]）；先拿两周真实数据再谈新形态（[V-Q5]）。人工干扰类由 P1-6 直接消源，其余类候选（流干净关闭无 done、token 中途轮换）留待日志对证（[V-Q5]，声明：未读守护全源码前是假设）。
- 验收：每次断流在日志中可归入已知分类；`doc/incident-2026-09-11.md`（[D2]）格式的后续事故有分类字段。

### P1-8 时间线「桥视角」（SSE + relay 同源 iframe）
- 数据不需要新采集：桥已在实时解码每一轮流（onPageCapture → decoder.js 归一化，[C18][C19]），内容实时可见=把已有数据推出去。
- 实现：relay 加 `/__webcode/turn-log` SSE 端点（路由挂法同现有 /__webcode/ 族 [C14]）；时间线 UI 由 relay 同源出一个页面塞 iframe——**不直接在面板页面 new EventSource**（受宿主 CSP connect-src 管辖会静默失败 [V-C]；frame-src 已被现有镜像 iframe 证明放行 [C12]；双 iframe 同源沙箱模式有 MCP Apps 规范背书 [R5]）。
- 命名纪律：时间线标「桥视角」（权威命题=桥解码到了什么），镜像标「站点视角」（=页面长什么样），两个命题不同不构成冲突；桥视角不得当「站点做了什么」的 ground truth（decoder 有损）（[V-Q3]）。
- 附加价值：5 个镜像不可达站点的唯一内容视图（[V-Q3]）；时间线回放同时是 Q5 分类日志的带标注失败数据源（[V-Q5]）。
- 验收：DeepSeek 真机一轮生成中，时间线随流出现思考/正文增量（真机留痕 [U4]）。

### P1-9 24h 双源 401 门禁 + 放开顺序
- 每站注入上线后观察 24h：驱动侧 401（尤其镜像活跃后）指向轮换回写缺失（P1-2），镜像侧 401 而驱动正常指向注入过期——归因不分开门禁等于没有（[V-Q1]）。
- 顺序：glm →（初始化后）doubao → qwen（对账后）→ kimi（登录后）；deepseek 恒为对照组不切换（[V-Q1]）。

## 4. P2 · 搁置记录（不立项，证据留档）

- 依用户硬约束搁置 screencast/截图路线（[U1]）；评审原判「缓议，仅当 P1 遥测证明看过程有真实需求后启动」（[V-裁定表]）。
- 已留档的工程储备（供未来重启时直接用，不必重新调研）：playwright-core 1.63 自带 `page.screencast.start({onFrame,size,quality})`（[T4]）；agent-browser 的 ack/push 双模式流水线与累计 ack 语义（[R1]）；CDP startScreencast 参数模板（[R1]）；评审 Q4 的两条真约束（ack 流水线、relay 事件循环背压）与「viewport 两视口不相干、都不动」的裁定（[V-Q4]）。
- WebRTC（Browserbase/Cloudflare Live View 类）维持备胎定位（[V-裁定表]；快照 [R4]）。

## 5. 明确例外与不救清单

- chatgpt/grok/gemini：本机网络 502——镜像和驱动同样致命，网络级问题任何路线都救不了（[O1][V-Q6]）。
- claude：地区受限（网络级 reset），独立窗口也活不了，不计入可救范围（[O1][V-Q6]）。
- steel-browser 参考仓库：本机三通道获取失败（git×2 early EOF、codeload 截断、npm registry 不通），价值随 P2 搁置降级，待网络好转重试（[T5]；记录于 reference/local-refs/2026-09-12-review-sources/README.md [R4]）。

## 6. 执行顺序

1. P0-0 存档 → P0-1 徽标 → P0-2 条件挂载 → P0-3 指纹（前三项都不需要重启 GUI 生效的验证方式：单测+本地 relay 实例）
2. P0-4 门禁（复跑+修复 client-render）
3. P1-0 spike → 定稿 → P1-1 glm → P1-2/P1-3 并行 → P1-4（视 P1-0）→ P1-5/P1-6 → P1-7 → P1-8 → P1-9 门禁观察
4. 每完成一项：真机验证留痕（[U4]）+ GEP Event 一行（[D3] 既有惯例）

## 7. 来源对照表

| # | 来源 | 位置/内容 |
| --- | --- | --- |
| U1 | 用户指令 2026-09-12 | 「会话中也能同步看到，但是必须是网页，不能截图中转路线」 |
| U2 | 用户指令 2026-09-12 | 「先写好plan，对应每个细节必须都有真实来源，这是唯一约束，然后文件放在根目录」 |
| U3 | 用户决定（09-12 早前会话） | 重启打断界面，pack→重装→重启时机留给用户 |
| U4 | 用户全局规则（memory real-machine-verification-first） | 真实调用/真机端到端优先于单测 |
| T1 | .tmp/loopback-secure-cookie-test.mjs | Edge 150.0.4078.83 headless，http://127.0.0.1:8977，四枚 Set-Cookie 全部落盘（document.cookie 回读含 __Secure-/__Host-） |
| T2 | .tmp/loopback-secure-cookie-test2.mjs | 同环境二次请求 Cookie 头回传全部三枚（含前缀） |
| T3 | T1 同脚本 ctx.cookies() 输出 | CDP 查询只返回 plain-t=ok3——回环 Secure cookie 的 CDP 查询面不全 |
| T4 | package/dsh-webcode-bridge/node_modules/playwright-core/types/types.d.ts:18621（另 :5845-5855） | `interface Screencast` + page.screencast 绑定，1.63.0 实装 |
| T5 | reference/ 落地记录（本会话） | agent-browser/wabac.js/node-http-proxy 克隆成功；steel-browser 三通道失败 |
| R1 | reference/agent-browser/cli/src/native/stream/websocket.rs:27-32,109-112；cdp_loop.rs:156-162 | ack 语义、累计 ack、startScreencast 参数 |
| R2 | reference/wabac.js/src/response.ts:89-105；examples/live-proxy/ | preset-cookie 无 jar 模式；实时代理+iframe+脚本注入蓝本 |
| R3 | reference/node-http-proxy/README.md:351-375 | cookieDomainRewrite/cookiePathRewrite 只动 domain/path 不动 Secure |
| R4 | reference/local-refs/2026-09-12-review-sources/ | 8 份文档快照 + README 索引（steel 获取失败记录） |
| R5 | R4 内 devto-chatgpt-iframe-sandbox.html + mcpui-protocol-details.html + mcp-apps-blog.html | 宿主/沙箱双 iframe 同源模式与 CSP 元数据 |
| V0 | reference/local-refs/2026-09-12-review-sources/external-review-2026-09-12.md | 外部评审全文存档（P0-0 落盘后生效） |
| V-Q1..Q6 / V-A..E / V-裁定表 / V-修正2 | 同 V0 | 评审六问答复、补充发现 A-E、路线裁定、修正 2 nuance |
| C1 | lib/mirror.js:198-203 | rewriteCookie 去 domain/secure、None→Lax |
| C2 | lib/mirror.js:430 | 上行转发 iframe Cookie 头 |
| C3 | lib/mirror.js:482,485-486 | 剥聚合 set-cookie 后 getSetCookie()+rewriteCookie 重写下发 |
| C4 | lib/mirror.js:214-241（:215） | bootstrap 注入 localStorage userToken（仅 DeepSeek） |
| C5 | lib/mirror.js:154-160；lib/relay.js:23-24,265 | loopbackOnly；host 默认 127.0.0.1:8931 |
| C6 | lib/browser-driver.js:1145-1163 | getToken 只认 deepseek userToken |
| C7 | lib/browser-driver.js:186-194 | webcode-login-state.json 落盘/回读 |
| C8 | lib/index.js:822-835 | driverStatus().sites 聚合（含缓存登录态） |
| C9 | lib/client.cjs:135-192 | 设置页 SiteAccounts 三态徽标（数据源 driver?.sites） |
| C10 | lib/client.cjs:329-367,388-403 | Conversation iframe 保活池/ensureFrame/sitebar（无徽标） |
| C11 | lib/client.cjs:14,219-222；lib/web-control.js:149-161 | api('status') 轮询先例；'GET status' 返回 driverStatus() |
| C12 | lib/client.cjs:413-420 | iframe sandbox 属性 |
| C13 | lib/index.js:857-868 | 独立窗口与自动化共用持久 profile 同一页面 |
| C14 | lib/index.js:873-883,919-941 | /__webcode/site/<id> 路由与多站点 mirror 实例 |
| C15 | lib/mirror.js:184-196 | browserFingerprintHeaders/DEFAULT_UA |
| C16 | lib/mirror.js:205-338 | bootstrap 脚本注入通道 |
| C17 | lib/browser-driver.js:445-463 | launchPersistentContext Edge/远调端口/viewport 640×900 |
| C18 | lib/browser-driver.js:24,35-107 | WEBCODE_SSE_DEBUG 取证；__wcCap 特征+500ms 守护 |
| C19 | lib/decoder.js:125-132；lib/browser-driver.js:802 | no_response_frames 产生与抛出链 |
| C20 | lib/index.js:806-810 | WEB_SESSION_LOST 重放首轮自愈 |
| C21 | package.json:58 + playwright-core types.d.ts:10227(cookies)/10099(addCookies) | cookie 导出/回写 API 实装确认 |
| C22 | lib/providers.js:170,35-50 | SITES 十站点；GLM 条目 |
| C23 | test/client-render.test.mjs、test/regression.test.mjs、test/mirror.test.mjs、test-mock/real-verify.mjs | 测试与 doctor 入口存在 |
| C24 | lib/client.cjs:460-475 | sidebarRightTabs 注册与 guide 文案 |
| C25 | lib/mirror.js:25-30,403-419 | STRIP_RESPONSE；静态转发+SSRF 防护 |
| D1 | PLAN.md | 0.12.0 路线图（断流分类/守护/Qwen v2 等既有契约） |
| D2 | doc/verify.md、doc/security-review.md、doc/review-guide.md、doc/incident-2026-09-11.md | 验收/安全审查/事故留痕入口 |
| D3 | .claude/GEP-skills/20-Events/2026-09-12-0.12.1-*.md | 本轮评估与实测 Event 留痕 |
| O1 | 09-12 真机观察（会话留痕/记忆） | 镜像未登录=glm/qwen/kimi/doubao/zai；502=chatgpt/grok/gemini；claude 地区受限 |
| O2 | 09-12 真机观察 | qwen 状态文件与实时接口说法不一 |
| O3 | 09-12 会话留痕（D3 亦载） | no_response_frames 长会话高频复发，收尾轮连续 3 次死于该错 |
| O4 | 09-12 下午 harness 留痕 | client-render 第二用例红（P0-4 先复跑定现状） |
| O5 | gitStatus + 会话哈希核对留痕 | 0.12.1 tgz 已 pack、workspace 未提交改动、已装副本为旧文案 |
